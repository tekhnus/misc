dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)

chan=$(./crsh-create-channels)

winid=$(kitty @ launch --location=before --cwd=${dir} bash -c "./crsh-server $chan $@")
kitty @ resize-window --self -a vertical -i -1000 2>/dev/null 
tput smcup
go run crsh-client.go $chan "$@"
tput rmcup
